function CycleQuotes() {
    var quotes = [
        "These aren't the droids you're looking for",
        "I am your father",
        "We're gonna need a bigger boat",
        "Houston we have a problem",
        "Don't get homeboy fired up",
    ];

    var quoteElement = document.getElementById('quote');

    Math.random()
}